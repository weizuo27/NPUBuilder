for i in NUM_IP:
    for j in IP_Combi(i):
        


def roundTableToGraphSol(roundSol, g, graphs):
    graphs.retriveOriginalGraph(g)
    sol = deepcopy(g)
    #Add information
    for n in sol.nodes():
        n.mappIP = #Has to be all filled in
        n.layerInfo =  #Has to be all filled in
